#include <iostream>
using namespace std;

int main (){
    int x = 5;   // declaration and initilization variable 
    cout << x << endl;

    x = 8; // updating the varible "x = 5 " to "x = 8", we are updating the value of the variable 

    cout << x << endl;

    x = x + 7;

    cout << x << endl;

    x = x - 5;
    cout << x << endl;

    x += 3;  // says this add 3 in x
    x -= 5;  // says this subtrack 5 in x
    return 0;
}