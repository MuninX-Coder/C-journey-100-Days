#include <iostream>
using namespace std;

int main (){
    // variables - variables are like a container where we can add values 

    int x;   // int x -  means create a box names as "x", where we can store integers, (int is a data type used to add integers only)  (declaration)
    x = 5;  // in this we put value 5 in the variable names as "x"  (initilization)
    cout << x << endl;
    // cout << x + 8 << endl;



    int y = 8;  // doing both declaration and initilization in one line.  

    cout << x + y << endl;


    int z , p ;
    z = 34; p = 43;

    cout << z + p << endl;
    return 0;
}